# ⚡ تشغيل سريع - نظام إدارة المواعيد مع الصلاحيات

## خطوات التشغيل السريع

### 1. إنشاء قاعدة البيانات
افتح phpMyAdmin وأنشئ قاعدة بيانات اسمها: `appointment_db`

### 2. تشغيل الأوامر
افتح Terminal في مجلد المشروع وشغل:

```bash
composer install
php artisan view:clear
php artisan config:clear
php artisan migrate
php artisan serve
```

### 3. فتح المتصفح
اذهب إلى: `http://localhost:8000`

## 👤 بيانات Super Admin

- **البريد الإلكتروني:** admin@admin.com
- **كلمة المرور:** admin123

## ✅ الميزات الجديدة!

### 🔐 نظام الصلاحيات:
- **المدير:** يمكنه إدارة جميع المستخدمين والمواعيد
- **المستخدم العادي:** يمكنه فقط إضافة وقراءة المواعيد (لا يمكن التعديل أو الحذف)

### 📋 إدارة المستخدمين:
- موافقة أو رفض المستخدمين الجدد
- ترقية المستخدمين إلى مدراء
- تخفيض رتبة المدراء
- حذف المستخدمين

### 🚪 تدفق التسجيل:
1. المستخدم الجديد ينشئ حساب
2. يتم تسجيل خروجه تلقائياً
3. يظهر له رسالة "في انتظار موافقة المدير"
4. المدير يوافق أو يرفض من لوحة التحكم
5. بعد الموافقة يمكن للمستخدم تسجيل الدخول

## 📱 الصفحات المتاحة:

### للجميع:
- الصفحة الرئيسية: `/`
- إنشاء حساب: `/register`
- تسجيل دخول: `/login`

### للمستخدمين المقبولين:
- لوحة التحكم: `/home`
- المواعيد: `/appointments`
- التقويم: `/calendar`

### للمدراء فقط:
- لوحة تحكم المدير: `/admin`
- إدارة المستخدمين: `/admin/users`

## 🔧 إذا واجهت مشاكل:

```bash
php artisan view:clear
php artisan config:clear
php artisan cache:clear
php artisan route:clear
```

---

**النظام جاهز مع نظام الصلاحيات الكامل! 🎉**