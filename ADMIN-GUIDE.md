# 👑 دليل Super Admin

## بيانات الدخول

- **البريد الإلكتروني:** admin@admin.com
- **كلمة المرور:** admin123

## صلاحيات Super Admin

### ✅ صلاحيات كاملة بدون قيود:

1. **إدارة المستخدمين:**
   - قبول أو رفض المستخدمين الجدد
   - ترقية المستخدمين إلى مدراء
   - تخفيض رتبة المدراء
   - حذف أي مستخدم
   - تعديل صلاحيات أي مستخدم

2. **إدارة المواعيد:**
   - عرض جميع المواعيد
   - إضافة مواعيد جديدة
   - تعديل أي موعد (حتى لو لم يكن من إنشائه)
   - حذف أي موعد
   - معرفة من أنشأ كل موعد

3. **الوصول الكامل:**
   - الوصول لجميع صفحات النظام
   - لوحة تحكم المدير
   - إدارة المستخدمين
   - جميع ميزات النظام

## صلاحيات المستخدم العادي

### 📋 صلاحيات محدودة:

1. **المواعيد:**
   - إضافة مواعيد جديدة
   - عرض جميع المواعيد
   - تعديل مواعيده فقط
   - حذف مواعيده فقط
   - لا يمكنه تعديل أو حذف مواعيد الآخرين

2. **القيود:**
   - لا يمكنه الوصول لصفحات الإدارة
   - لا يمكنه إدارة المستخدمين
   - يحتاج موافقة المدير للدخول

## كيفية إدارة المستخدمين

### قبول مستخدم جديد:
1. اذهب إلى `/admin` أو `/admin/users`
2. ستجد المستخدمين في انتظار الموافقة
3. اضغط زر "قبول" ✅

### رفض مستخدم:
1. اذهب إلى `/admin/users`
2. اضغط زر "رفض" ❌

### ترقية مستخدم إلى مدير:
1. اذهب إلى `/admin/users`
2. اضغط زر "ترقية" ⬆️
3. سيصبح المستخدم مديراً بصلاحيات كاملة

### تخفيض رتبة مدير:
1. اذهب إلى `/admin/users`
2. اضغط زر "تخفيض" ⬇️
3. سيعود إلى مستخدم عادي

### حذف مستخدم:
1. اذهب إلى `/admin/users`
2. اضغط زر "حذف" 🗑️
3. سيتم حذف المستخدم وجميع مواعيده

## معرفة من أنشأ الموعد

في صفحة المواعيد `/appointments`:
- يظهر عمود "المنشئ" يوضح اسم من أنشأ الموعد
- إذا كان المنشئ مديراً، يظهر badge أحمر "مدير"
- إذا كان مستخدماً عادياً، يظهر badge أزرق "مستخدم"

في صفحة تفاصيل الموعد:
- يظهر اسم منشئ الموعد بوضوح
- يظهر نوع المستخدم (مدير/مستخدم)

## ملاحظات مهمة

⚠️ **تحذيرات:**
- لا يمكن حذف المدير الوحيد في النظام
- عند حذف مستخدم، تُحذف جميع مواعيده تلقائياً
- المدير يمكنه تعديل وحذف أي موعد بغض النظر عن من أنشأه

✅ **مميزات:**
- النظام آمن ومحمي
- جميع الإجراءات مسجلة
- واجهة سهلة الاستخدام
- تصميم عربي متجاوب

---

**Super Admin لديه صلاحيات كاملة بدون أي قيود! 👑**