# 🚀 دليل التثبيت والتشغيل

## المتطلبات الأساسية

قبل البدء، تأكد من توفر:
- PHP >= 7.4
- Composer
- MySQL >= 5.7
- XAMPP أو WAMP (للويندوز)

## خطوات التثبيت

### 1. إعداد قاعدة البيانات

افتح phpMyAdmin وأنشئ قاعدة بيانات جديدة:

```sql
CREATE DATABASE appointment_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 2. تثبيت التبعيات

افتح Terminal في مجلد المشروع وشغل:

```bash
composer install
```

### 3. إعداد ملف البيئة

الملف `.env` موجود بالفعل، تأكد من الإعدادات التالية:

```env
APP_NAME="Laravel Appointment System"
APP_URL=http://localhost:8000

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=appointment_db
DB_USERNAME=root
DB_PASSWORD=
```

**ملاحظة:** إذا كان لديك كلمة مرور لـ MySQL، ضعها في `DB_PASSWORD`

### 4. إنشاء مفتاح التطبيق (إذا لزم الأمر)

```bash
php artisan key:generate
```

### 5. تشغيل الهجرات (Migrations)

```bash
php artisan migrate
```

إذا ظهرت رسالة تطلب تأكيد إنشاء قاعدة البيانات، اكتب `yes`

### 6. تنظيف الـ Cache

```bash
php artisan config:clear
php artisan route:clear
php artisan cache:clear
php artisan view:clear
```

### 7. تشغيل الخادم

```bash
php artisan serve
```

الآن افتح المتصفح واذهب إلى: `http://localhost:8000`

## 🎯 الاستخدام السريع

### إنشاء حساب جديد

1. اذهب إلى: `http://localhost:8000/register`
2. أدخل:
   - الاسم الكامل
   - البريد الإلكتروني
   - كلمة المرور
3. اضغط "إنشاء الحساب"

### تسجيل الدخول

1. اذهب إلى: `http://localhost:8000/login`
2. أدخل البريد الإلكتروني وكلمة المرور
3. اضغط "تسجيل الدخول"

### إضافة موعد جديد

1. بعد تسجيل الدخول، اذهب إلى "المواعيد"
2. اضغط "إضافة موعد جديد"
3. أدخل:
   - اسم الموعد
   - تاريخ ووقت البداية
   - تاريخ ووقت النهاية
4. اضغط "حفظ الموعد"

## 🔧 حل المشاكل الشائعة

### مشكلة: "SQLSTATE[HY000] [1049] Unknown database"

**الحل:**
- تأكد من إنشاء قاعدة البيانات `appointment_db` في phpMyAdmin
- تأكد من اسم قاعدة البيانات في ملف `.env`

### مشكلة: "Class not found"

**الحل:**
```bash
composer dump-autoload
php artisan config:clear
php artisan cache:clear
```

### مشكلة: "No application encryption key"

**الحل:**
```bash
php artisan key:generate
```

### مشكلة: "Target class [Controller] does not exist"

**الحل:**
```bash
php artisan route:clear
php artisan config:clear
```

## 📱 الصفحات المتاحة

بعد التشغيل، يمكنك الوصول إلى:

- **الصفحة الرئيسية:** `http://localhost:8000/`
- **تسجيل الدخول:** `http://localhost:8000/login`
- **إنشاء حساب:** `http://localhost:8000/register`
- **لوحة التحكم:** `http://localhost:8000/home`
- **المواعيد:** `http://localhost:8000/appointments`
- **التقويم:** `http://localhost:8000/calendar`

## 🎨 الميزات

✅ نظام مصادقة كامل (تسجيل دخول وإنشاء حساب)
✅ إدارة المواعيد (إضافة، تعديل، حذف، عرض)
✅ تقويم تفاعلي لعرض المواعيد
✅ فلترة المواعيد (قادمة، منتهية، اليوم)
✅ إحصائيات في لوحة التحكم
✅ تصميم عربي متجاوب
✅ واجهة مستخدم حديثة وجميلة

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تأكد من تشغيل XAMPP/WAMP
2. تأكد من تشغيل MySQL
3. تأكد من إنشاء قاعدة البيانات
4. راجع ملف `.env`

---

**تم التطوير بـ ❤️ باستخدام Laravel 8**