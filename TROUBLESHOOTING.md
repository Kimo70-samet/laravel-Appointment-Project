# 🔧 حل المشاكل الشائعة

## مشكلة: BadMethodCallException - Call to undefined method App\User::isPending()

### السبب:
النظام كان يستخدم نموذج User القديم بدلاً من الجديد المحدث.

### الحل المطبق:
1. ✅ حذف ملف `app/User.php` القديم
2. ✅ تحديث `config/auth.php` لاستخدام `App\Models\User`
3. ✅ تنظيف جميع أنواع الـ cache
4. ✅ إعادة إنشاء المستخدم الإداري

### إذا واجهت نفس المشكلة:

```bash
# 1. تنظيف الـ cache
php artisan config:clear
php artisan route:clear
php artisan view:clear
php artisan cache:clear

# 2. إعادة تشغيل الخادم
php artisan serve
```

### بيانات المدير الافتراضي:
- **البريد:** admin@appointment.com
- **كلمة المرور:** admin123

## مشاكل أخرى محتملة:

### مشكلة: صفحة بيضاء عند تسجيل الدخول
**الحل:**
```bash
php artisan view:clear
php artisan config:clear
```

### مشكلة: خطأ في قاعدة البيانات
**الحل:**
```bash
php artisan migrate:fresh
# ثم إعادة إنشاء المستخدم الإداري
```

### مشكلة: لا يمكن الوصول لصفحات الإدارة
**تأكد من:**
1. تسجيل الدخول بحساب المدير
2. أن المستخدم له صلاحيات admin
3. أن الـ middleware يعمل بشكل صحيح

---

**جميع المشاكل تم حلها! النظام يعمل بكفاءة 🚀**